import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  search: {
    position: "absolute",
    top: 40,
    left: 70,
    alignItems: "center",
    zIndex: 10,
  },
});
export default styles;
