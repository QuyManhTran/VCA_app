import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  search: {
    position: "absolute",
    top: 40,
    left: 30,
    right: 0,
    width: "100%",
    alignItems: "center",
    zIndex: 10,
  },
});
export default styles;
